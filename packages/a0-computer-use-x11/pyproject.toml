[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "a0-computer-use-x11"
version = "1.5"
description = "Reserved X11 computer-use backend package for a0"
requires-python = ">=3.10"
authors = [
    { name = "agent0ai" },
]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/a0_computer_use_x11"]
